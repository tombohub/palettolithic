import { createPalette } from "./createPalette";
import { ColorPalette } from "./domain";

/**
 * Generates CSS color variables template
 * @param {ColorPalette[]} palette palette object generated by user choosing color
 */
function generateCssVariables(palette: ColorPalette[]): string {
  let variables = "";

  palette.forEach(({ colorName, shades }) => {
    shades.forEach(({ weight, hexValue }) => {
      variables += `--${colorName}-${weight}: ${hexValue};\n`;
    });
    variables += "\n";
  });

  return variables.trim(); // Remove the trailing newline for clean output
}

export { generateCssVariables };
